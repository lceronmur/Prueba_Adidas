# Copy to .env and adjust. Every value has a default, so the project runs
# fine without this file.

PORT=3000
DB_PATH=./data/inventory.db

# Demo admin key: reads everything and configures thresholds.
# In production this would be hashed and rotatable, not a plain shared secret.
ADMIN_API_KEY=sk_admin_demo
